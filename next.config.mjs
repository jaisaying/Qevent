/** @type {import('next').NextConfig} */
const nextConfig = {
//mene add kiya
    images: {
        unoptimized: true,  // ✅ Image optimization disable karega
      },
};

export default nextConfig;
